校园网常规情况下都会选择记住密码，此时的cookie中会保存加密过后的密码（固定值，很容易获取到），然后每次提交的数据包中都是交加密过后的密码，最后参数是否加密也是选择true。然后在我们登录之初url后续的get包含大量参数，url中使用的是明码，在post的数据包中是将这些内容两次url编码上传的，参数的内容都是固定的，也很容易获取到且要注意到：不同的浏览器的wlanuserip参数是不一样的。从而导致的数据包长度也是不一样的，在写数据包的时候要注意。